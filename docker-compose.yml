services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: laravel_app

    restart: unless-stopped

    ports:
      - "8080:80"

    environment:
      # Laravel Configuration
      APP_ENV: production
      APP_DEBUG: "false"
      APP_KEY: "base64:79diLxbM3/y28VpmB5guR0XKk8FSz8BMGXgW3qu8ko="

      # Internal MariaDB Configuration
      DB_CONNECTION: mysql
      DB_HOST: "10.0.1.112"
      DB_PORT: 3306
      DB_DATABASE: devops
      DB_USERNAME: root
      DB_PASSWORD: "Zain@12345"

    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
